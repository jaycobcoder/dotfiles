# 테스트 범위

- **CUD만 테스트한다.** 조회(Read)는 이 컨벤션의 대상에서 제외한다.
- 각 CUD 메서드는 다음 케이스를 반드시 다뤄야 한다:

| 연산 | 필수 케이스 |
|------|------------|
| Create | 성공, 유효성 실패, 중복(unique 제약) |
| Update | 성공, 존재하지 않는 엔티티, 낙관적 잠금(optimistic-lock) 충돌(해당 시) |
| Delete | 성공, 존재하지 않는 엔티티, 연관 데이터 제약(해당 시) |
